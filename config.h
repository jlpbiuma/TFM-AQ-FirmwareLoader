#ifndef CONFIG_H
#define CONFIG_H

#define SSID "{SSID}"
#define PASSWORD "{PASSWORD}"
#define MQTT_BROKER "{MQTT_BROKER}"
#define MQTT_PORT {MQTT_PORT}
#define MQTT_TOPIC "estacion/{ID_ESTACION}"

#endif
